# Environment variables (API Keys)
.env

# Virtual Environment
venv/
env/

# Python cache
__pycache__/
*.pyc

# Local Data (Requirements and outputs)
data/
# Ignore all files in the output folder
output/*
# But don't ignore the .gitkeep file (this keeps the folder in Git)
!output/.gitkeep